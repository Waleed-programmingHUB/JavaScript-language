// Alerts & Prompt in Javascript

// Alerts in In-browser
alert("Javascript Learning") // Does not return Anything

// Prompt in JS
let n = prompt('What is your name','');

// Confirm in JS
let del = confirm('Do you really want to delete this post');

// If-Else conditional
if (del) {
    console.log('You delete it');
} else {
    console.log('You cancel the delete');
}
// print to developer console
console.log(n);
console.log(del);

